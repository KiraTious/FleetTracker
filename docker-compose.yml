services:
  db:
    build:
      context: .
      dockerfile: db/Dockerfile
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-fleettracker}
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "55432:5432"

  web:
    build: .
    depends_on:
      - db
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:postgres@db:5432/fleettracker}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:-super-secret-key}
      FLASK_APP: ${FLASK_APP:-app.py}
    ports:
      - "5000:5000"
    command: bash -c "python seed_db.py && gunicorn -b 0.0.0.0:5000 app:app"

volumes:
  db_data:
